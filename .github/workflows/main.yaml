name : Build and Deploy Backend 

on:
  push:
    branches:
      - feature/cicd-pipeline
   workflow_dispatch:


jobs:
  build-and-deploy:
    runs-on: ubuntu-latest


    steps:
     - name: Checkout code
       uses: actions/checkout@v4 
